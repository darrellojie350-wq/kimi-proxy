{
  "name": "kimi-proxy-app",
  "version": "1.0.0",
  "private": true,
  "description": "Kimi Proxy — premium Kimi Code CLI client (PWA wrapped as Android APK)",
  "scripts": {
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
